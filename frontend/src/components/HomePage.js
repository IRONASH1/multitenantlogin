import React from 'react';

const HomePage = () => {
    return (
        <div>
            {/* The welcome message will be handled in `HomePageWithSubdomain` */}
        </div>
    );
};

export default HomePage;
